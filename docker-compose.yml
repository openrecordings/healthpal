version: '3'

services:
  db:
    image: postgres
  web:
    build: .
    command: ./start.sh
    volumes:
      - .:/code
      - gems:/gems
    ports:
      - "3000:3000"
    depends_on:
      - db

volumes:
  gems:
    driver: local
