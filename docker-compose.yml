version: '3'
services:
  db:
    image: orals_db
  web:
    build: .
    command: ./start.sh
    volumes:
      - .:/code
    ports:
      - "3000:3000"
    depends_on:
      - db
  gems:
    image: ruby:2.4-stretch
    volumes:
      - /gems
