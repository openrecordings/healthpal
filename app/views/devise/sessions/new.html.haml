.centered-form-container
  .form-input-container
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .form-field-container
        .form-label.invisible= t(:email_address)
        = f.email_field :email, autofocus: true, placeholder: t(:email_address), class: 'form-input'
        .input-underscore
      .form-field-container
        .form-label.invisible= t(:password)
        = f.password_field :password, autocomplete: 'off', placeholder: t(:password), class: 'form-input'
        .input-underscore
      .form-field-container.full-center
        = f.submit t(:log_in), class: 'button', id: 'login-button'
      .form-field-container
        = link_to t(:forgot_password), new_user_password_path, class: 'blue-link'
