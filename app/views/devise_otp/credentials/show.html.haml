.ic3d_small_form
  %h2= I18n.t('title', {:scope => 'devise.otp.submit_token'})
  %p Two-factor authentication is enabled for your account.
  = form_for(resource, :as => resource_name, :url => [resource_name, :otp_credential], :html => { :method => :put }) do |f|
    = f.hidden_field :challenge, {:value => @challenge}
    = f.hidden_field :recovery, {:value => @recovery}
    %p Please enter your one-time password below
    = f.text_field :token, :autocomplete => :off, :autofocus => true, :size => 6, :value => ''
    %p.pad-top-20= f.submit I18n.t('submit', {:scope => 'devise.otp.submit_token'}), class: 'btn btn-primary'
