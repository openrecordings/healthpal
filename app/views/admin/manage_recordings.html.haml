.table-container
  = link_to 'Manually upload audio file', file_upload_path, class: 'btn btn-primary'
  %br
  %br
  %table.table.table-striped.tablesorter{:"data-sort-column" => 0, :"data-sort-desc" => 1}
    %thead
      %tr
        %th Recording created at
        %th Recorded By
        %th Tags
    %tbody
      - @recordings.all.each do |recording|
        - if recording.utterances.any?
          %tr.trecording-select
            %td.recording-select{data: {href: play_path(recording.id)}}= link_to format_date_time_sort(recording.created_at), play_path(recording.id)
            %td.recording-select{data: {href: play_path(recording.id)}}= recording.user.email
            %td= link_to 'Manage tags and links', new_tag_path(id: recording.id)
