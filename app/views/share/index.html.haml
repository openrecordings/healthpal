.centered-form-container
  %div
    .form-title
      Share Recordings
    = submit_tag 'Add a person to share with', class: 'button', id: 'open-share-form'
  #new-share-form.hidden
    .form-title
      Enter an email address for the person you want to share with
    = form_tag url: shares_path do
      .form-field-container
        .form-label
          Email address
        = email_field :email, nil, class: 'form-input'
      .form-field-container
        .form-label
          Re-enter email address
        = email_field :email, nil, class: 'form-input'
      = submit_tag 'Share with this person', class: 'button', id: 'new-share-submit'

-# - if @shares.any?
-#   .panel.panel-default.table-responsive.margin-top-40
-#     .center#sharing-table-header You are sharing your recordings with these people
-#     %table.table.table-striped
-#       %thead
-#         %th Name
-#         %th Email address
-#       %tbody
-#         - @shares.each do |s|
-#           %tr
-#             %td= s.shared_with.full_name
-#             %td
-#               =s.shared_with.email
-#               %span.table-button= link_to 'Stop sharing with this person', 'javascript:;', class: 'btn btn-primary revoke-share', data: {'share-id': s.id}
