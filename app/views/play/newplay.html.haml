#play-view
  #search-and-select
    #minimized
      foo
    #search
      = render 'search'
    #select
      - @recordings.each do |recording|
        = render 'recording_list_item', recording: recording
  #playback
    #notes
      Notes
    #transport
      Transport