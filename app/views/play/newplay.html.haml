#play-view
  #search-and-select
    #search
      = render 'search'
    #select
      - @recordings.each do |recording|
        = render 'recording_list_item', recording: recording
  #playback.collapse
    #notes
      Notes
    #transport
      Transport
