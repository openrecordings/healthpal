#video-container
#play-view{'data-initial-recording-id': @recording&.id}
  #metadata-overlay.overlay.invisible
    .modal-form-container
      %form#metadata-form
        .form-title.margin-bottom-10 Edit recording details
        .form-label Appointment name
        = text_field_tag('edit-recording-title', nil, value: @recording&.title, placeholder: 'Recording name', class: 'form-input', data: {placeholder: 'Recording name'})
        .input-underscore
        .form-label Doctor's name
        = text_field_tag('edit-recording-provider', nil, value: @recording&.provider, placeholder: "Doctor's name", class: 'form-input', data: {placeholder: "Doctor's name"})
        .input-underscore
        .save-cancel-buttons
          #metadata-cancel.cancel Cancel
          #metadata-save.save Save

  #left
    #search-and-select
      #select-header Select a recording
      #select
        - @recordings.each do |recording|
          = render 'recording_list_item', recording: recording
  #right
    #player-container
      #header
        #header-left
          #recording-title
          #metadata-edit-icon
            #recording-provider
            = inline_svg('edit.svg', class: 'edit-svg')
        #header-right
          #recording-date
          #recording-days-ago

      #notes.full-center
        #no-notes-message.full-center
          Your notes for this recording will appear here
      #player
        #timeline-container
          #timeline.track
          #progress-bar
          #playhead.track
        #time
          %span#current-time
            00:00
          %span#duration
            00:00
        #player-button-container
          #rewind-button.player-button-left.track
            = inline_svg('skip_to_start.svg', class: 'player-button-svg-left')
            .player-button-label-left= t(:rewind)
          #back-button.player-button.track
            = inline_svg('step_back.svg', class: 'player-button-svg')
            .player-button-label= t(:back_10_sec )
          #play-pause-button.player-button.track
            = inline_svg('play.svg', id: 'play-glyph', class: 'player-button-svg')
            = inline_svg('pause.svg', id: 'pause-glyph', class: 'player-button-svg hidden')
            #play-label.player-button-label= t(:play)
            #pause-label.player-button-label.hidden= t(:pause)
          #forward-button.player-button.track
            = inline_svg('step_forward.svg', class: 'player-button-svg')
            .player-button-label= t(:forward_10_sec)
          #mute-button.player-button-right.track
            = inline_svg('volume_off.svg', id: 'mute-glyph', class: 'player-button-svg-right')
            = inline_svg('volume_max.svg', id: 'unmute-glyph', class: 'player-button-svg-right hidden')
            #mute-label.player-button-label-right= t(:mute)
            #unmute-label.player-button-label-right.hidden= t(:unmute)
      #toolbar
        #show-select
          = inline_svg('arrow-left.svg', class: 'small-svg')
          %span.text-with-svg Select
        #create-note
          %span.text-with-svg New note
          = inline_svg('plus.svg', class: 'small-svg')