#play-view{'data-recording-id': @recording&.id}
  #left.hidden
    #search-and-select
      #search
        = render 'search'
      #select
        - @recordings.each do |recording|
          = render 'recording_list_item', recording: recording
  #right
    #playback
      #video-element.hidden
      #header
        #show-select Select a recording
      #notes
        Notes
      #transport
        -# Transport