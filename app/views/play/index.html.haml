#play-view(data-recording-id="#{nil}")
  #left.hidden
    #search-and-select
      #search
        = render 'search'
      #select
        - @recordings.each do |recording|
          = render 'recording_list_item', recording: recording
  #right
    #playback
      #header
        #show-select Select a recording
      #notes
        Notes
      #transport
        -# Transport