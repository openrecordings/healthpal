.recording-list-item.log(id="select-recording-#{recording.id}" data-recording-id="#{recording.id}" data-recording-year="#{recording.created_at.year}" data-recording-month="#{recording.created_at.month}")
  .recording-description
    .recording-title= recording.title
    - if recording.provider
      .recording-provider.italic-note= recording.provider
  .recording-date
    .recording-date= minimal_date(recording.created_at)
    .recoording-days-ago.italic-note= days_ago(recording.created_at)